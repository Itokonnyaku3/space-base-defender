/**
 * ゲーム世界の共有定数（単一情報源）。
 * マップサイズや中心座標など、複数モジュールから参照する値はここに集約する。
 * 直書きの数値リテラルを増やさないこと（ARCHITECTURE_REVIEW.md §3.1 参照）。
 */

/** ワールド（マップ）の一辺のサイズ (px)。物理境界・カメラ境界・星空生成で使用。 */
export const WORLD_SIZE = 6000;

/** ワールド中心座標 (px)。自基地が置かれるマップ中央。 */
export const WORLD_CENTER = WORLD_SIZE / 2; // 3000
